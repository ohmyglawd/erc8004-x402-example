# Deployments

This folder contains JSON files with deployed contract addresses per network.

- `localhost.json` is generated by `npm run deploy:localhost`
- `base-sepolia.json` is generated by `npm run deploy:baseSepolia`

These files are also synced into the dapp at `dapp/src/config/deployments/*`.
